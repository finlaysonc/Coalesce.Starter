
/// <reference path="../coalesce.dependencies.d.ts" />

// Knockout List View Model for: Department
// Auto Generated by IntelliTect.Coalesce

var baseUrl = baseUrl || '';

module ListViewModels {

    // Add an enum for all methods that are static and IQueryable
    export enum DepartmentDataSources {
            Default,
        }
    export class DepartmentList extends Coalesce.BaseListViewModel<DepartmentList, ViewModels.Department> {
        protected modelName = "Department";

        protected apiController = "/Department";

        public modelKeyName = "departmentID";
        public dataSources = DepartmentDataSources;
        public itemClass = ViewModels.Department;

        public query: {
            where?: string;
            departmentID?:number;
            name?:String;
            budget?:number;
            startDate?:moment.Moment;
            instructorID?:number;
        } = null;

        // The custom code to run in order to pull the initial datasource to use for the collection that should be returned
        public listDataSource: DepartmentDataSources = DepartmentDataSources.Default;

        public static coalesceConfig = new Coalesce.ListViewModelConfiguration<DepartmentList, ViewModels.Department>(Coalesce.GlobalConfiguration.listViewModel);
        public coalesceConfig = new Coalesce.ListViewModelConfiguration<DepartmentList, ViewModels.Department>(DepartmentList.coalesceConfig);

        // Valid values
        public administratorValidValues: KnockoutObservableArray<any> = ko.observableArray([]);
        public loadAdministratorValidValues: (callback: any) => void;
    
        protected createItem = (newItem?: any, parent?: any) => new ViewModels.Department(newItem, parent);

        constructor() {
            super();
        }
    }

    export namespace DepartmentList {
        // Classes for use in method calls to support data binding for input for arguments
    }
}